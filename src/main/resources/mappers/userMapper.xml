<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.team.sprout.dao.StompMapper">

<select id="selectUser" parameterType="string" resultType="User">
	select
		member_num
		,stomp_id
		,stomp_pw
		,stomp_nm
	from
		membertest
	where
		stomp_id = #{userId}
</select>

<insert id="insertcr" parameterType="ChatRoom">
	insert into chatRoomtest
	(
	chatRoom_num
	, chatRoom_name
	)
	values
	(
	#{chatRoom_num}
	, #{chatRoom_name}
	)
</insert>
</mapper>